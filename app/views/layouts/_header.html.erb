<nav class="bg-whie shadow">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-20 justify-between">
      <div class="flex">
        <div class="flex">
          
        <%= image_tag 'logo.svg' , class: 'w-2/3 flex flex-shrink-0 font-medium text-3xl items-center' %>

        </div>
      </div>
      <div class="ml-6 flex items-center gap-4">
        <% if user_signed_in? %>
        <%= link_to 'Add Note', new_note_path, class: 'border border-black rounded-md py-2 px-6 hover:ml-1 hover:-mt-1 hover:border-b-4 hover:border-r-4' %>
        <%= link_to 'Log Out', destroy_user_session_path, data: {"turbo-method": :delete} , class: 'border border-black rounded-md py-2 px-6 hover:ml-1 hover:-mt-1 hover:border-b-4 hover:border-r-4' %>

        <% else %>
        <%= link_to 'Log In', new_user_session_path, class: 'border border-black rounded-md py-2 px-6 hover:ml-1 hover:-mt-1 hover:border-b-4 hover:border-r-4' %>
        <% end %>


      </div>
    </div>
  </div>
</nav>